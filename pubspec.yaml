name: import_generator
description: A Dart CLI tool to generate a single exports file from your lib folder
version: 0.0.1
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  yaml: ^3.1.2
  path: ^1.8.3
executables:
  import_generator: import_generator
